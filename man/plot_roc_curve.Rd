% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_roc.R
\name{plot_roc_curve}
\alias{plot_roc_curve}
\alias{plot.roc}
\title{Plot an Empirical ROC Curve}
\usage{
plot_roc_curve(
  p,
  p0,
  fpr = seq(0, 1, by = 0.01),
  col = "black",
  xlim = c(0, 1),
  ylim = c(0, 1),
  main = "",
  lty = 1,
  lwd = 2
)

\method{plot}{roc}(x, ...)
}
\arguments{
\item{p}{Numeric scores under the alternative or positive class.}

\item{p0}{Numeric scores under the null or negative class.}

\item{fpr}{False-positive-rate values in `[0, 1]`.}

\item{col, lty, lwd}{Graphical parameters passed to [graphics::plot()].}

\item{xlim, ylim}{Axis limits.}

\item{main}{Plot title.}

\item{x}{Scores for the legacy `plot.roc()` S3 method.}

\item{...}{Additional arguments passed to [plot_roc_curve()].}
}
\value{
Invisibly, a data frame containing `fpr`, `threshold`, and `tpr`.
}
\description{
Uses empirical null-score quantiles as thresholds. The curve treats smaller
scores as stronger evidence for the positive class.
}
\examples{
set.seed(12345)
null <- rnorm(100)
alternative <- rnorm(100, mean = -1)
roc <- plot_roc_curve(alternative, null)
head(roc)
}

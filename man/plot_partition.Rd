% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_part.R
\name{plot_partition}
\alias{plot_partition}
\alias{plot.part}
\title{Plot a Two-Dimensional Partition}
\usage{
plot_partition(
  xy.part,
  border = FALSE,
  xlim = c(0, 1),
  ylim = c(0, 1),
  zlim = grDevices::extendrange(xy.part[, "den"]),
  main = "",
  nlevels = 100,
  plot.den = TRUE,
  color.fun = grDevices::terrain.colors,
  plot.scale = TRUE,
  color = NA,
  newpage = TRUE
)

\method{plot}{part}(x, ...)
}
\arguments{
\item{xy.part}{A matrix or data frame with columns `xmin`, `xmax`, `ymin`,
`ymax`, and `den`.}

\item{border}{`FALSE` uses the fill colors as borders, `TRUE` uses black, and
a single color string uses that border color.}

\item{xlim, ylim}{Horizontal and vertical plotting limits.}

\item{zlim}{Density limits used to map `den` to colors.}

\item{main}{Plot title.}

\item{nlevels}{Number of colors in the palette; at least two.}

\item{plot.den}{Whether to color cells by `den`.}

\item{color.fun}{A function that returns `nlevels` colors.}

\item{plot.scale}{Whether to draw a density color scale.}

\item{color}{Optional fixed fill color. The default `NA` uses `color.fun`.}

\item{newpage}{Whether to start a new grid graphics page.}

\item{x}{A partition object for the legacy `plot.part()` S3 method.}

\item{...}{Additional arguments passed to [plot_partition()].}
}
\value{
Invisibly, the clipped partition data with an added `fill` column.
}
\description{
Draws rectangular partition cells with optional density-based fill colors.
This helper uses grid graphics and is intended for two-dimensional HMAP or
posterior partitions after their integer bounds have been converted to the
desired plotting coordinates.
}
\examples{
cells <- data.frame(
  xmin = c(0, 0.5), xmax = c(0.5, 1),
  ymin = c(0, 0), ymax = c(1, 1), den = c(0.6, 1.4)
)
plot_partition(cells, plot.scale = FALSE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apt.R
\name{opt}
\alias{opt}
\title{Fit an Optional Pólya Tree}
\usage{
opt(
  X,
  Xpred = NULL,
  Omega.type = "unit",
  max.resol = 10,
  rho0 = 0.5,
  rho0.mode = 0,
  n.post.samples = 0
)
}
\arguments{
\item{X}{A numeric vector, matrix, or data frame. Rows are observations and
columns are dimensions.}

\item{Xpred}{Optional prediction locations. A vector is interpreted as many
locations for univariate data and as one location for multivariate data.
If `NULL`, no predictive densities are computed.}

\item{Omega.type}{Sample-space construction: `"unit"` uses the unit
hypercube and `"standardized"` uses a slightly padded empirical range of
`X` and `Xpred` in each dimension.}

\item{max.resol}{Maximum partition depth. Must be an integer from 1 to 14.}

\item{rho0}{Baseline prior probability of complete shrinkage. Must lie in
`[0, 1]`.}

\item{rho0.mode}{How the complete-shrinkage probability changes with level:
`0` keeps it constant; `1` uses
`1 - (1 - rho0) / (level + 1)^2`; and `2` uses
`1 - (1 - rho0) / 2^level`.}

\item{n.post.samples}{Number of posterior partition samples to draw.}
}
\value{
A list with the same structure as [apt()].
}
\description{
A convenience wrapper around [apt()] that uses one non-stopping state with
zero beta precision, reproducing the optional Pólya tree (OPT).
}
\examples{
set.seed(12345)
x <- rbeta(60, 4, 7)
fit <- opt(x, Xpred = c(0.25, 0.5, 0.75), max.resol = 4)
fit$predictive_densities
}
\references{
Wong, W. H. and Ma, L. (2010). Optional Pólya tree and Bayesian inference.
*The Annals of Statistics*, 38(3), 1433--1459.
\doi{10.1214/09-AOS755}.
}
\seealso{
[apt()], [cond.opt()]
}

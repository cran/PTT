% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cond_apt.R
\name{cond.apt}
\alias{cond.apt}
\title{Fit a Conditional Adaptive Pólya Tree}
\usage{
cond.apt(
  X,
  Y,
  Xpred = NULL,
  Ypred = NULL,
  OmegaX.type = "unit",
  OmegaY.type = "unit",
  max.resX = 5,
  max.resY = 8,
  rho0.X = 0.2,
  rho0.mode.X = 0,
  rho0.Y = 0.2,
  rho0.mode.Y = 0,
  tran.mode = 1,
  lognu.lb = -1,
  lognu.ub = 4,
  n.grid = 5,
  n.s = 5,
  beta = 0.1,
  n.post.samples = 0
)
}
\arguments{
\item{X, Y}{Numeric vectors, matrices, or data frames containing paired
predictor and response observations. They must have the same number of
rows.}

\item{Xpred, Ypred}{Optional paired predictor and response locations at which
to evaluate the conditional predictive density. Supply both or neither;
they must have the same number of rows.}

\item{OmegaX.type, OmegaY.type}{Sample-space construction for predictors and
responses. See `Omega.type` in [apt()].}

\item{max.resX, max.resY}{Maximum partition depths for predictor and response
spaces, each between 1 and 14.}

\item{rho0.X, rho0.Y}{Baseline prior complete-shrinkage probabilities for the
predictor partition and response model.}

\item{rho0.mode.X, rho0.mode.Y}{Level-dependent stopping modes for the
predictor partition and response model. See `rho0.mode` in [apt()].}

\item{tran.mode}{Shrinkage-state transition model. `0` makes non-stopping
states independent across nodes; `1` imposes stochastically increasing
shrinkage and treats complete shrinkage separately; `2` applies the
increasing transition kernel to all states, including complete shrinkage.}

\item{lognu.lb, lognu.ub}{Lower and upper support bounds for `log10(nu)`,
where `nu` is the beta precision (shrinkage) parameter.}

\item{n.grid}{Number of midpoint quadrature points within each shrinkage
state.}

\item{n.s}{Number of non-stopping shrinkage states.}

\item{beta}{Nonnegative stickiness parameter in the exponential transition
kernel. `beta = 0` gives uniform probability over admissible higher states.}

\item{n.post.samples}{Number of posterior partition samples to draw.}
}
\value{
A list containing `logrho`, `logphi`, the predictor-space
  `part_points_hmap`, `predictive_densities`, `OmegaX`, and `OmegaY`. If
  requested, `part_points_post_samples` contains sampled predictor
  partitions. Partition bounds use integer cell indices at depth `max.resX`.
}
\description{
Fits the two-stage conditional-density model of Ma (2017): the predictor
space is recursively partitioned and an adaptive Pólya tree models the
response distribution within each predictor block.
}
\examples{
set.seed(12345)
x <- runif(80)
y <- rbeta(80, 2 + 6 * x, 8 - 5 * x)
fit <- cond.apt(
  x, y, Xpred = c(0.25, 0.75), Ypred = c(0.3, 0.7),
  max.resX = 3, max.resY = 4, n.grid = 3, n.s = 3
)
fit$predictive_densities
}
\references{
Ma, L. (2017). Recursive partitioning and multi-scale modeling on
conditional densities. *Electronic Journal of Statistics*, 11(1),
1297--1325. \doi{10.1214/17-EJS1254}.
}
\seealso{
[cond.opt()], [apt()]
}
